<nav class="bg-white shadow-lg">
  <div class="max-w-7xl mx-auto px-4">
    <div class="flex justify-between h-16">
      <div class="flex items-center">
        <span class="text-xl font-bold text-indigo-600">CRM Employee</span>
      </div>
      <div class="flex items-center space-x-4">
        <% if (user) { %>
          <span class="text-gray-700 font-medium"><%= user.name %></span>
          <a href="/profile" class="text-gray-600 hover:text-gray-900 flex items-center">
            <i class="fas fa-user mr-1"></i> Profile
          </a>
          <a href="/auth/logout" class="text-gray-600 hover:text-gray-900 flex items-center">
            <i class="fas fa-sign-out-alt mr-1"></i> Logout
          </a>
        <% } else { %>
          <a href="/auth/login" class="text-gray-600 hover:text-gray-900 flex items-center">
            <i class="fas fa-sign-in-alt mr-1"></i> Login
          </a>
        <% } %>
      </div>
    </div>
  </div>
</nav> 